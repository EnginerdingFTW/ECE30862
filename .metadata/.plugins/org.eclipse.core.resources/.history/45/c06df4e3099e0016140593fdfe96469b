import java.util.Timer;

import FiredShot.DeathTimer;

import java.util.Timer;
/**
    A Grub is a Creature that moves slowly on the ground.
*/
public class Grub extends Creature {

    public Grub(Animation left, Animation right,
        Animation deadLeft, Animation deadRight)
    {
        super(left, right, deadLeft, deadRight);
    }
    
    private void creatPlayerWatcher()
    {
    	Timer tim = new Timer();
		tim.schedule(new DeathTimer(), 0);
    }
    
    private class PlayerWatcher extends java.util.TimerTask
    {
	    public void run()
	    {
	    	try
	    	{
	    		Thread.sleep((long) (deathTimer * 1000));
	    	}
	    	catch (Exception e)
	    	{
	    		System.out.println("ERROR");
	    	}
	    	setState(STATE_DEAD);
	    }
    }


    public float getMaxSpeed() {
        return 0.05f;
    }

}
